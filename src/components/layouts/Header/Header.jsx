import { SearchIcon, ProfileIcon } from "@/components/icons"


export const Header = () => {
    return (
        <header>
            <SearchIcon />
            <ProfileIcon />
        </header>
    )
}